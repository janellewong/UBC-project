{
	"title": "(WHERE = {}) SELECT courses_uuid, courses_dept, courses_id",
	"input": {
		"WHERE": {},
		"OPTIONS": {
			"COLUMNS": [
				"courses_uuid",
				"courses_dept",
				"courses_id"
			]
		}
	},
	"errorExpected": true,
	"with": "ResultTooLargeError"
}
