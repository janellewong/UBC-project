{
	"title": "SELECT courses_uuid, courses_dept, courses_id, courses_avg WHERE courses_avg < 0",
	"input": {
		"WHERE": {
			"LT": {
				"courses_avg": 0
			}
		},
		"OPTIONS": {
			"COLUMNS": [
				"courses_uuid",
				"courses_dept",
				"courses_id",
				"courses_avg"
			],
			"ORDER": "courses_avg"
		}
	},
	"errorExpected": false,
	"with": []
}
