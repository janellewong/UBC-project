{
	"title": "(WHERE: null) SELECT courses_uuid, courses_dept, courses_id",
	"input": {
		"WHERE": null,
		"OPTIONS": {
			"COLUMNS": ["courses_uuid", "courses_dept", "courses_id"]
		}
	},
	"errorExpected": true,
	"with": "InsightError"
}
